# https://github.com/typst/packages

[package]
name = "invoicing"
version = "1.0.0"
entrypoint = "src/lib.typ"
# renovate: image=ghcr.io/typst/typst
compiler = "0.11.0"
authors = ["Léo Colombaro <@LeoColomb>"]
license = "MIT"
description = ""
repository = "https://github.com/LeoColomb/dotdocs"
keywords = []
categories = [
  "office",
]
disciplines = []
exclude = [
	"tests",
]

[template]
path = "template"
entrypoint = "main.typ"
thumbnail = "thumbnail.png"
